{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "setup": "npm run update && npm run permission && npm run key && npm run activate",
        "activate": "cd /etc/apache2/sites-available && a2ensite web.conf && service apache2 reload",
        "key": "php artisan key:generate",
        "permission": "chmod -R 777 .",
        "update": "composer update && npm update",
        "db": "php artisan migrate:fresh && php artisan db:seed"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
